homepage: "https://www.productfruits.com/"
versions:
  # Each entry maps a released gallery version to the git commit that contains
  # the corresponding template.tpl. After you commit template.tpl, replace the
  # sha below with that commit's full hash (git rev-parse HEAD).
  - sha: 5c09c3548562746791270e57841f55bac151d305
    changeNotes: Initial release
